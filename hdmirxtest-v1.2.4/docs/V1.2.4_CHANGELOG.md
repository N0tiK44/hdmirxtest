# V1.2.4

- Makes foreground operation the only normal video workflow.
- Adds the root-level `PiCycle.sh`; running it with no argument starts video.
- Treats `Ctrl+C` as the expected clean stop and returns the terminal.
- Adds one-command `setup.sh` for dependencies, validation, build and EDID setup.
- Disables and removes the older persistent passthrough systemd service.
- Keeps only the verified boot-time EDID loader enabled.
- Updates all primary instructions for the repository's nested version folder.
- Keeps the repository `README.md` separate from the replaceable release folder.
